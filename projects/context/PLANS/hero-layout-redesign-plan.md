# 首屏布局改动计划方案（修订版）

## 📋 方案概述

**方案名称**: HeroSection 首屏布局重新设计（学习成果展示导向）
**创建日期**: 2026-02-04
**最后更新**: 2026-02-04
**状态**: 📝 待执行
**优先级**: 🔴 高

### 改动背景
当前项目首屏以 ClueBoard 产品介绍为核心，强调产品功能和价值主张。但根据项目定位（个人作品展示 + 学习复盘 + 项目路演），首屏应该首先突出"阿林的AI编程之旅"这一学习成果展示的主题，将 ClueBoard 作为一个实践案例来呈现，而非直接的产品介绍页。

### 改动目标
1. **调整首屏定位**：从产品介绍页转向学习成果展示页，将 ClueBoard 作为学习成果的案例呈现
2. **强化个人品牌**：突出"阿林的AI编程之旅"主题
3. **保留产品展示**：ClueBoard 作为学习实践案例嵌入首屏引言
4. **避免 AI 套路**：打破常见的设计模式，创造独特美学
5. **保持章节顺序**：首屏之后章节顺序不变（PainPoints → MVPDemo → CompetitorComparison → LearningJourney → TechStackRoadmap → CallToAction）

---

## 📚 设计参考

### 参考 SKILL.md 设计指南

根据 `SKILL.md` 的指导原则，本设计遵循以下理念：

#### 1. 设计思维
- **目的**：展示 AI 编程学习成果，以 ClueBoard 为案例
- **基调**：选择"杂志/编辑风"与"极简主义"结合的风格，体现专业性和个人风格
- **差异化**：避免常见的 AI 套路（Inter 字体、紫色渐变、陈词滥调的配色）

#### 2. 前端美学指南

**排版**：
- 避免使用 Inter、Roboto 等通用字体
- 使用独特的展示字体与精致的正文字体配对
- 大标题使用粗体、高对比度设计
- 建议字体组合：
  - 主标题：`Space Grotesk` 或 `Outfit`（Google Fonts）
  - 正文：`JetBrains Mono`（代码感）或 `DM Sans`（现代感）

**色彩与主题**：
- 避免白色背景上的紫色渐变
- 使用强烈的点缀色配合主导色彩
- 建议配色方案：
  ```css
  :root {
    /* 主色调 - 暖色系，营造学习氛围 */
    --primary-bg: #fefce8;        /* 浅黄 yellow-50 */
    --accent-primary: #ea580c;    /* 暖橙 orange-600 */
    --accent-secondary: #16a34a;  /* 绿色 green-600 */
    
    /* 文字颜色 - 高对比度 */
    --text-primary: #0f172a;      /* 深蓝 gray-900 */
    --text-secondary: #475569;    /* 中灰 gray-600 */
    --text-quote: #94a3b8;        /* 浅灰 gray-400 */
    
    /* 便签颜色 - 自然柔和 */
    --note-yellow: #fef08a;
    --note-blue: #bfdbfe;
    --note-green: #86efac;
    --note-pink: #fbcfe8;
  }
  ```

**动效**：
- 使用一次精心编排的页面加载配合交错揭示
- 避免分散的微交互
- 滚动触发制造惊喜

**空间构成**：
- 不对称布局
- 对角线流动
- 充足的负空间

**背景与视觉细节**：
- 渐变网格背景
- 噪点纹理
- 几何图案
- 分层透明度

#### 3. AI 产品展示页设计模式

**Hero 区域设计**：
- 简洁有力的主标题（≤ 15 字）
- 清晰的价值主张（≤ 30 字）
- 强烈的 CTA 按钮
- 视觉化产品演示（截图、动画或交互预览）

**文案写作指南**：
- 避免技术术语堆砌
- 使用场景化的描述
- 主标题：一句话说清"这个产品能为你做什么"
- 副标题：补充说明使用场景或核心优势

---

## 🎨 新首屏布局设计

### 布局线框图

```
┌─────────────────────────────────────────────────────────────────┐
│  Hero Section (首屏) - 新设计                                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                           │   │
│  │            探索AI：学习、实践、展示                        │   │
│  │                  阿林的AI编程之旅                          │   │
│  │                                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                           │   │
│  │  [ 渐变网格背景 + 噪点纹理 + 浮动元素 ]                    │   │
│  │                                                           │   │
│  │  ┌───────────────────────────────────────────┐           │   │
│  │  │                                           │           │   │
│  │  │     "总忘长期任务？不是你记性差，        │           │   │
│  │  │      是工具太笨重。"                      │           │   │
│  │  │                                           │           │   │
│  │  │     这是我用 AI 辅助开发的第一个产品    │           │   │
│  │  │     记录从点子到上线的完整过程          │           │   │
│  │  │                                           │           │   │
│  │  └───────────────────────────────────────────┘           │   │
│  │                                                           │   │
│  │              [ ↓ 向下滚动查看完整案例 ]                   │   │
│  │                                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 设计细节

#### 1. 顶部标题区域（简洁白色背景）
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│        探索AI：学习、实践、展示                          │
│           阿林的AI编程之旅                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**特点**：
- 极简设计，充足的负空间
- 小标题使用 `Space Grotesk` 字体，大写，字间距 0.2em
- 主标题使用 `Outfit` 字体，超大字号，加粗
- 添加微妙的底部分割线（1px 实线，浅灰色）

#### 2. 背景区域（暖色系渐变网格）
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│        [ 渐变网格背景 + 噪点纹理 + 浮动元素 ]            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**背景设计**：
- 基础背景：浅黄色渐变（`from-yellow-50 to-orange-50`）
- 叠加渐变网格图案（CSS `conic-gradient` 或 `radial-gradient`）
- 添加噪点纹理（SVG 滤镜或 CSS 混合模式）
- 浮动元素：代码片段、便签、图标（低透明度 0.1-0.15）

**CSS 实现**：
```css
.hero-background {
  background: linear-gradient(135deg, #fefce8 0%, #ffedd5 100%);
  position: relative;
}

/* 渐变网格 */
.hero-background::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(circle at 20% 50%, rgba(234, 88, 12, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(22, 163, 74, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, rgba(59, 130, 246, 0.03) 0%, transparent 50%);
  background-size: 100% 100%;
}

/* 噪点纹理 */
.hero-background::after {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0.5;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  mix-blend-mode: overlay;
}
```

#### 3. 核心文案区域（居中卡片式设计）
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  ┌───────────────────────────────────────────┐         │   │
│  │                                           │         │   │
│  │     "总忘长期任务？不是你记性差，        │         │   │
│  │      是工具太笨重。"                      │         │   │
│  │                                           │         │   │
│  │     这是我用 AI 辅助开发的第一个产品    │         │   │
│  │     记录从点子到上线的完整过程          │         │   │
│  │                                           │         │   │
│  └───────────────────────────────────────────┘         │   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**特点**：
- 使用白色卡片容器，带柔和阴影和圆角
- 引语使用大号引号装饰（`text-8xl`，浅灰色）
- 主标题使用超大字号，高对比度
- 副标题说明这是学习案例
- 添加微妙的渐变边框或装饰线条

#### 4. 向下滚动提示
```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│              [ ↓ 向下滚动查看完整案例 ]                  │   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**特点**：
- 文案改为"向下滚动查看完整案例"
- 添加下划线装饰
- 箭头使用自定义动画（淡入 + 位移）
- 悬停时轻微放大

---

## 📐 具体改动清单

### 一、HeroSection 组件改动（唯一改动）

#### 1.1 组件结构调整
**改动前**：
```tsx
<section id="hero" className="relative min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 overflow-hidden">
  <p className="text-sm md:text-base text-gray-400 mb-6 tracking-wider uppercase">
    探索AI：学习、实践、展示
  </p>
  <h1 className="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 leading-tight">
    总忘长期任务？
  </h1>
  <p className="text-2xl md:text-4xl lg:text-5xl font-semibold text-gray-300 mb-8 leading-tight">
    不是你记性差，是工具太笨重
  </p>
  <p className="text-lg md:text-xl text-gray-400 mb-12 max-w-2xl mx-auto leading-relaxed">
    为健忘星人打造的「线索墙式」任务管理工具<br />
    让重要的事情，一眼就能看见
  </p>
</section>
```

**改动后**：
```tsx
<section id="hero" className="relative min-h-screen flex flex-col overflow-hidden">
  {/* 顶部标题区域 */}
  <div className="pt-20 pb-8 bg-white border-b border-gray-100">
    <div className="max-w-7xl mx-auto px-4">
      <p className="text-sm md:text-base text-gray-500 mb-3 tracking-widest uppercase text-center font-['Space_Grotesk']">
        探索AI：学习、实践、展示
      </p>
      <h1 className="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-900 text-center font-['Outfit']">
        阿林的AI编程之旅
      </h1>
    </div>
  </div>

  {/* 背景区域 */}
  <div className="flex-1 relative bg-gradient-to-br from-yellow-50 to-orange-50 overflow-hidden hero-background">
    {/* 浮动元素 */}
    <div className="absolute inset-0 overflow-hidden">
      {floatingElements.map((element, index) => (
        <motion.div
          key={index}
          className={`absolute ${element.className}`}
          style={{
            left: `${element.left}%`,
            top: `${element.top}%`,
            opacity: element.opacity,
          }}
          animate={{
            y: [0, element.yRange, -element.yRange, 0],
            rotate: [0, element.rotate, -element.rotate, 0],
          }}
          transition={{
            duration: element.duration,
            delay: element.delay,
            repeat: Infinity,
            ease: 'easeInOut',
          }}
        >
          {element.icon}
        </motion.div>
      ))}
    </div>

    {/* 核心文案 */}
    <div className="absolute inset-0 flex items-center justify-center">
      <div className="max-w-4xl mx-auto px-4">
        <div className="bg-white/80 backdrop-blur-sm rounded-3xl shadow-2xl p-8 md:p-12 lg:p-16 border border-white/50">
          {/* 引号 */}
          <div className="flex justify-start mb-6">
            <span className="text-8xl text-gray-200 font-serif leading-none">"</span>
          </div>
          
          {/* 主标题 */}
          <p className="text-3xl md:text-4xl lg:text-5xl font-bold text-gray-900 leading-tight mb-8 font-['Outfit']">
            总忘长期任务？<br />
            不是你记性差，是工具太笨重。
          </p>
          
          {/* 副标题（案例说明） */}
          <div className="pt-6 border-t border-gray-200">
            <p className="text-lg md:text-xl text-gray-600 leading-relaxed font-['DM_Sans']">
              这是我用 AI 辅助开发的第一个产品
            </p>
            <p className="text-base md:text-lg text-gray-500 mt-2 font-['DM_Sans']">
              记录从点子到上线的完整过程
            </p>
          </div>
          
          {/* 引号（右侧） */}
          <div className="flex justify-end mt-6">
            <span className="text-8xl text-gray-200 font-serif leading-none">"</span>
          </div>
        </div>
      </div>
    </div>

    {/* 向下滚动提示 */}
    <div className="absolute bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center gap-3 cursor-pointer group">
      <span className="text-sm text-gray-600 tracking-wide font-['Space_Grotesk']">
        向下滚动查看完整案例
      </span>
      <motion.div 
        animate={{ y: [0, 8, 0] }} 
        transition={{ duration: 1.5, repeat: Infinity, ease: 'easeInOut' }}
        className="w-6 h-6 rounded-full border-2 border-gray-400 flex items-center justify-center group-hover:scale-110 transition-transform"
      >
        <motion.div 
          animate={{ y: [0, 6, 0] }}
          transition={{ duration: 1.5, repeat: Infinity, ease: 'easeInOut' }}
        >
          <ChevronDown className="w-4 h-4 text-gray-600" />
        </motion.div>
      </motion.div>
    </div>
  </div>

  {/* 底部渐变过渡 */}
  <div className="absolute bottom-0 left-0 right-0 h-24 bg-gradient-to-t from-white to-transparent pointer-events-none" />
</section>
```

#### 1.2 浮动元素配置
```typescript
const floatingElements = [
  {
    icon: <Code className="w-12 h-12" />,
    className: "bg-orange-100 text-orange-600 rounded-lg p-3 shadow-lg",
    left: 10,
    top: 20,
    opacity: 0.12,
    yRange: 30,
    rotate: -5,
    duration: 8,
    delay: 0,
  },
  {
    icon: <Lightbulb className="w-10 h-10" />,
    className: "bg-yellow-100 text-yellow-600 rounded-full p-3 shadow-lg",
    left: 85,
    top: 15,
    opacity: 0.15,
    yRange: 25,
    rotate: 5,
    duration: 7,
    delay: 1,
  },
  {
    icon: <FileText className="w-11 h-11" />,
    className: "bg-green-100 text-green-600 rounded-lg p-3 shadow-lg",
    left: 15,
    top: 70,
    opacity: 0.1,
    yRange: 20,
    rotate: -3,
    duration: 6,
    delay: 2,
  },
  {
    icon: <Layers className="w-10 h-10" />,
    className: "bg-blue-100 text-blue-600 rounded-full p-3 shadow-lg",
    left: 80,
    top: 75,
    opacity: 0.12,
    yRange: 28,
    rotate: 4,
    duration: 9,
    delay: 0.5,
  },
];
```

#### 1.3 字体加载
在 `layout.tsx` 中添加字体加载：
```tsx
import { Outfit, Space_Grotesk, DM_Sans } from 'next/font/google'

const outfit = Outfit({ 
  subsets: ['latin'],
  weight: ['400', '500', '600', '700', '800', '900'],
  variable: '--font-outfit',
  display: 'swap',
})

const spaceGrotesk = Space_Grotesk({ 
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-space-grotesk',
  display: 'swap',
})

const dmSans = DM_Sans({ 
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-dm-sans',
  display: 'swap',
})
```

#### 1.4 CSS 全局样式
在 `globals.css` 中添加：
```css
:root {
  --font-outfit: var(--font-outfit);
  --font-space-grotesk: var(--font-space-grotesk);
  --font-dm-sans: var(--font-dm-sans);
  
  /* 主色调 - 暖色系 */
  --hero-bg-start: #fefce8;
  --hero-bg-end: #ffedd5;
  --accent-primary: #ea580c;
  --accent-secondary: #16a34a;
  
  /* 文字颜色 */
  --text-primary: #0f172a;
  --text-secondary: #475569;
  --text-quote: #e5e7eb;
}

/* Hero 背景样式 */
.hero-background::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(circle at 20% 50%, rgba(234, 88, 12, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(22, 163, 74, 0.03) 0%, transparent 50%),
    radial-gradient(circle at 40% 80%, rgba(59, 130, 246, 0.03) 0%, transparent 50%);
  pointer-events: none;
}

.hero-background::after {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0.4;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  mix-blend-mode: overlay;
  pointer-events: none;
}
```

#### 1.5 样式调整要点
- **背景渐变**：从深色渐变改为暖色系渐变（`from-yellow-50 to-orange-50`）
- **文字颜色**：从白色改为深灰色（`text-gray-900`）
- **浮动元素**：使用代码图标、灯泡、文档等元素，低透明度（0.1-0.15）
- **字体**：使用独特的字体组合（Outfit + Space Grotesk + DM Sans）
- **卡片设计**：白色毛玻璃卡片，柔和阴影

#### 1.6 动画调整
- 保留原有的循环动画
- 浮动元素透明度降低（0.1-0.15）
- 添加文字渐入动画（fade-in-up）
- 保持向下箭头跳动动画
- 添加悬停效果（轻微放大）

---

### 二、其他组件（不改动）

#### 2.1 章节顺序保持不变
```tsx
export default function Home() {
  return (
    <div className="min-h-screen bg-white">
      <NavigationBar />
      <main>
        <HeroSection /> {/* 改动 */}
        <PainPointsSection /> {/* 不改 */}
        <MVPDemoSection /> {/* 不改 */}
        <CompetitorComparisonSection /> {/* 不改 */}
        <LearningJourneySection /> {/* 不改 */}
        <TechStackRoadmapSection /> {/* 不改 */}
        <CallToActionSection /> {/* 不改 */}
      </main>
      <Footer />
    </div>
  );
}
```

#### 2.2 导航栏保持不变
```tsx
const navItems: NavItem[] = [
  { id: 'hero', label: '首页', icon: <Home className="w-4 h-4" />, href: '#hero' },
  { id: 'pitch', label: '项目路演', icon: <Lightbulb className="w-4 h-4" />, href: '#pitch' },
  { id: 'learning', label: '学习复盘', icon: <BookOpen className="w-4 h-4" />, href: '#learning' },
  { id: 'contact', label: '联系我', icon: <Mail className="w-4 h-4" />, href: '#contact' },
];
```

#### 2.3 页面元数据更新（可选）
```tsx
export const metadata: Metadata = {
  title: '阿林的AI编程之旅 | 学习成果展示',
  description: '记录我从零开始学习AI编程的完整过程，包括学习心得、实践项目和成长路径。ClueBoard是我的第一个实践项目，展示了如何用AI辅助开发一个完整的产品。',
};
```

---

## 🎨 配色方案

### 新配色方案（暖色系 + 高对比度）
```css
:root {
  /* 主色调 - 暖色系，营造学习氛围 */
  --primary-bg: #fefce8;        /* 浅黄 yellow-50 */
  --primary-bg-end: #ffedd5;    /* 浅橙 orange-50 */
  
  /* 强调色 - 强烈的点缀色 */
  --accent-primary: #ea580c;    /* 暖橙 orange-600 */
  --accent-secondary: #16a34a;  /* 绿色 green-600 */
  --accent-blue: #3b82f6;       /* 蓝色 blue-500 */
  
  /* 文字颜色 - 高对比度 */
  --text-primary: #0f172a;      /* 深蓝 gray-900 */
  --text-secondary: #475569;    /* 中灰 gray-600 */
  --text-quote: #e5e7eb;        /* 浅灰 gray-200 */
  --text-muted: #9ca3af;        /* 灰色 gray-400 */
  
  /* 便签颜色 - 自然柔和 */
  --note-yellow: #fef08a;       /* 黄色 yellow-200 */
  --note-blue: #bfdbfe;         /* 蓝色 blue-200 */
  --note-green: #86efac;        /* 绿色 green-200 */
  --note-pink: #fbcfe8;         /* 粉色 pink-200 */
  
  /* 功能色 */
  --success: #22c55e;
  --warning: #f59e0b;
  --error: #ef4444;
}
```

---

## 📱 响应式设计

### 桌面端 (1024px+)
- 顶部标题区域高度：160px
- 背景区域高度：calc(100vh - 160px)
- 核心文案字体：text-5xl（主标题）
- 卡片内边距：p-16

### 平板端 (640px - 1024px)
- 顶部标题区域高度：140px
- 背景区域高度：calc(100vh - 140px)
- 核心文案字体：text-4xl（主标题）
- 卡片内边距：p-12

### 移动端 (< 640px)
- 顶部标题区域高度：120px
- 背景区域高度：calc(100vh - 120px)
- 核心文案字体：text-3xl（主标题）
- 卡片内边距：p-8

---

## 🔄 动画效果

### 1. 顶部标题区域
- 进入动画：fade-in-down（从上方淡入）
- 延迟：0s
- 持续时间：0.6s

### 2. 背景浮动元素
- 保留原有的循环动画
- 透明度：0.1-0.15
- 颜色：使用暖色系（橙色、黄色、绿色、蓝色）
- 持续时间：6-9秒

### 3. 核心文案卡片
- 进入动画：fade-in-up（从下方淡入）+ scale-in（缩放淡入）
- 延迟：0.2s
- 持续时间：0.8s
- 悬停效果：轻微放大（scale 1.02）

### 4. 向下滚动提示
- 进入动画：fade-in（淡入）
- 延迟：1s
- 持续时间：0.5s
- 持续动画：bounce（垂直跳动）
- 悬停效果：放大（scale 1.1）

---

## 📝 实施步骤

### 阶段一：准备阶段（30分钟）
1. **确认设计方案**：确认首屏布局设计
2. **准备资源**：
   - 选择字体（Outfit, Space Grotesk, DM Sans）
   - 准备图标（Code, Lightbulb, FileText, Layers）
3. **创建备份**：备份当前的 HeroSection.tsx 文件

### 阶段二：字体配置（15分钟）
1. **修改 layout.tsx**：添加字体加载
2. **修改 globals.css**：添加 CSS 变量和全局样式
3. **测试字体加载**：确保字体正常显示

### 阶段三：HeroSection 开发（1-2小时）
1. **重构 HeroSection.tsx**：
   - 调整组件结构（顶部标题 + 背景区域）
   - 更新样式和配色
   - 调整动画效果
2. **实现浮动元素**：
   - 配置浮动元素数组
   - 实现循环动画
3. **实现核心文案卡片**：
   - 白色毛玻璃卡片
   - 大号引号装饰
   - 渐进式文字揭示

### 阶段四：测试阶段（30分钟）
1. **视觉测试**：
   - 桌面端视觉效果检查
   - 平板端视觉效果检查
   - 移动端视觉效果检查
2. **功能测试**：
   - 导航跳转功能测试
   - 平滑滚动功能测试
   - 响应式布局测试
3. **性能测试**：
   - 页面加载速度测试
   - 动画流畅度测试
   - 字体加载测试

### 阶段五：优化阶段（30分钟）
1. **细节优化**：
   - 调整字体大小和间距
   - 优化动画效果
   - 优化配色方案
2. **跨浏览器测试**：
   - Chrome 测试
   - Firefox 测试
   - Safari 测试
   - Edge 测试

### 阶段六：文档更新（15分钟）
1. **更新 HeroSection 组件文档**
2. **更新线框图文档**
3. **更新全局摘要**（可选）

**总计时间：3-4小时**

---

## ⚠️ 注意事项

### 1. 字体加载
- 使用 Google Fonts 的 `display: swap` 策略
- 确保字体加载不阻塞页面渲染
- 测试字体加载性能

### 2. 性能优化
- 使用 CSS 渐变而非图片背景
- 动画使用 `transform` 和 `opacity` 属性（GPU 加速）
- 避免过多的浮动元素（4-6个即可）

### 3. 兼容性
- 确保改动在所有现代浏览器中正常显示
- 移动端适配需要特别关注
- 动画效果需要考虑性能较低的设备

### 4. 可访问性
- 确保文字对比度符合 WCAG 标准（4.5:1）
- 添加适当的 ARIA 标签（如需要）
- 测试键盘导航

### 5. 保持不变的部分
- **不要修改其他章节组件**（PainPointsSection, MVPDemoSection 等）
- **不要修改章节顺序**
- **不要修改导航栏顺序**
- **只修改 HeroSection**

---

## 📊 预期效果

### 改动前
- 首屏焦点：ClueBoard 产品介绍
- 视觉风格：深色渐变 + 白色文字
- 用户感知：这是一个产品推广页
- 情感共鸣：较弱（以功能为主）

### 改动后
- 首屏焦点：阿林的AI编程之旅（学习成果展示）
- 视觉风格：暖色系 + 高对比度 + 独特字体
- 用户感知：这是一个学习成果展示页
- 情感共鸣：较强（以个人故事为主）
- 产品展示：作为学习案例自然呈现

---

## 🎯 成功标准

1. **视觉层面**：
   - 首屏主题清晰，突出"阿林的AI编程之旅"
   - 背景效果美观，不干扰内容阅读
   - 配色方案和谐统一（暖色系 + 高对比度）
   - 字体独特且易读

2. **内容层面**：
   - ClueBoard 作为学习案例自然呈现
   - 文案清晰传达学习成果主题
   - 向下滚动引导明确

3. **技术层面**：
   - 响应式布局正常
   - 动画流畅不卡顿
   - 页面加载速度无明显下降
   - 字体加载正常

4. **用户体验层面**：
   - 用户能快速理解网站定位
   - 导航流畅自然
   - 视觉冲击力强，避免 AI 套路

---

## 📅 时间估算

| 阶段 | 预估时间 | 备注 |
|------|---------|------|
| 阶段一：准备阶段 | 30分钟 | 确认设计、准备资源 |
| 阶段二：字体配置 | 15分钟 | 添加字体加载 |
| 阶段三：HeroSection 开发 | 1-2小时 | 核心代码改动 |
| 阶段四：测试阶段 | 30分钟 | 多端测试 |
| 阶段五：优化阶段 | 30分钟 | 细节调整 |
| 阶段六：文档更新 | 15分钟 | 更新相关文档 |
| **总计** | **3-4小时** | 不含设计和评审时间 |

---

## 🔗 相关文档

- [SKILL.md](../../SKILL.md) - 前端设计指南
- [HeroSection 组件文档](../modules/sections/hero-section.md)
- [项目布局线框图](../wireframes/project-layout-wireframe.md)
- [全局摘要](../_global.md)
- [路由映射表](../routes.json)

---

## 💡 后续建议

### 短期（本次改动完成后）
1. 收集用户反馈
2. 根据反馈微调细节
3. 更新相关文档

### 中期（1-2周后）
1. 考虑增加更多个人作品展示
2. 添加学习日志/博客功能
3. 优化 SEO 和分享功能

### 长期（1个月后）
1. 考虑添加评论/互动功能
2. 增加学习资源推荐
3. 建立个人品牌矩阵

---

## ✅ 验收检查清单

### HeroSection 改动
- [ ] 顶部标题区域正确显示
- [ ] 背景效果正常工作（渐变网格 + 噪点纹理）
- [ ] 浮动元素正常显示并循环动画
- [ ] 核心文案卡片样式正确
- [ ] 引号装饰正常显示
- [ ] 向下滚动提示正常
- [ ] 字体加载正常（Outfit + Space Grotesk + DM Sans）
- [ ] 配色方案符合设计要求
- [ ] 响应式布局正常
- [ ] 动画效果流畅

### 保持不变
- [ ] 章节顺序未改动
- [ ] 导航栏顺序未改动
- [ ] 其他组件未改动

### 测试通过
- [ ] 桌面端测试通过
- [ ] 平板端测试通过
- [ ] 移动端测试通过
- [ ] 浏览器兼容性测试通过
- [ ] 性能测试通过
- [ ] 无控制台错误

### 文档更新
- [ ] HeroSection 组件文档已更新（可选）
- [ ] 线框图文档已更新（可选）

---

## 🔄 方案修订记录

| 版本 | 日期 | 修订内容 | 修订人 |
|------|------|---------|--------|
| v1.0 | 2026-02-04 | 初始版本，包含章节顺序调整 | AI Assistant |
| v2.0 | 2026-02-04 | 修订版：保持章节顺序不变，只修改首屏，参考 SKILL.md 设计指南 | AI Assistant |

---

**方案版本**: v2.0
**最后更新**: 2026-02-04
**方案状态**: 📝 待用户确认
**重要变更**: 保持章节顺序不变，只修改首屏，参考 SKILL.md 设计指南
